# 安全策略

Nexus Office Agent 处理多租户身份、企业数据、模型调用和代码执行边界，安全问题请优先私下报告，不要在公开 Issue 中粘贴凭据、日志、生产 URL 或可复现的越权细节。

## 报告内容

请提供：

- 受影响的版本或提交；
- 最小化复现步骤和影响范围；
- 是否涉及跨租户、Sandbox 逃逸、Secret、Tool/MCP 越权、身份绕过或数据泄露；
- 已采取的临时缓解措施。

如果 GitHub 仓库已启用 Security Advisories，请使用私密漏洞报告；否则请联系仓库维护者的私有渠道。不要把真实凭据作为复现材料发送。

## 安全默认值

- 本地和生产环境均不得把 Secret 写入源码、文档、日志、模型上下文或 Git 历史；
- 生产默认关闭演示身份和未配置的执行能力；
- 未知 Tool、未批准资源、缺少租户/Run scope、审批对象漂移和外部依赖不可用时失败关闭；
- 真实生产使用前必须完成微 VM、网络出口、RLS、密钥、审计、灾备和回退验收。

## 凭据误提交

如果凭据误入工作区或 Git 历史：立即停止使用并轮换凭据，清理仓库历史，并在报告中说明暴露范围。仅从工作树删除文件不能撤销已经泄露的凭据。
